{
  "version": "0.1.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "args": [
        ""
      ],
      "envFile": "${workspaceFolder}/.env"
    },
    {
      "name": "Test",
      "type": "go",
      "request": "launch",
      "mode": "test",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "args": [
        ""
      ],
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
